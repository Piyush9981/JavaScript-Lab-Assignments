let city = "Haridwar"; // String
let population = 300000; // Number
let isCapital = false; // Boolean
let mayor; // Undefined
let previousMayor = null; // Null

console.log(typeof city, typeof population, typeof isCapital, typeof mayor, typeof previousMayor);

// Task 3.1
    let name = "Piyush" //String
    let age = 20; //Number
    let isMarried = false //Boolean
    let course; //Undefine
    let job = null; //Null
    let speedOfLight = 3n //bigint
    let studentId = Symbol("SECRET_12345") //symbol

    console.log(name, ":", typeof name,"|", age, ":", typeof age,"|", isMarried, ":", typeof isMarried,"|",course, ":", typeof course,"|",job, ":", typeof job,"|",speedOfLight, ":", typeof speedOfLight,"|",studentId, ":", typeof studentId);
